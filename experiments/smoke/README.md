# Smoke

Runs the smallest deterministic end-to-end validation for preprocessing, training, inversion, checkpointing, evaluation, metrics export, and plotting.

Run it with:

```bash
bash experiments/smoke/run_all.sh
```

Expected artifacts land in one timestamped `results/<timestamp>__smoke__cpu_smoke/` directory.
